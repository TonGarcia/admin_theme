require 'spec_helper'

describe AdminMaterialize do
  it 'has a version number' do
    expect(AdminMaterialize::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
